package com.zygisk.kavya_kanaja.ui.theme

import androidx.compose.ui.graphics.Color

// Yellow Palette - "Middle Version"
val YellowPrimary = Color(0xFFFFEB3B) // Middle Yellow
val YellowSecondary = Color(0xFFFFF176) // Lighter Yellow for accents
val YellowTertiary = Color(0xFFFFF59D) // Pale Yellow for subtle elements

// Container Colors
val YellowPrimaryContainer = Color(0xFFFFD600).copy(alpha = 0.3f)
val YellowSecondaryContainer = Color(0xFFFFF176).copy(alpha = 0.25f)

// Darker version for Light Mode readability
val YellowPrimaryDark = Color(0xFFFBC02D) // Slightly deeper but still "Middle" yellow for contrast

// Additional UI Colors
val AppBlack = Color(0xFF000000)
val AppWhite = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF121212)
val SurfaceVariantDark = Color(0xFF1E1E1E)
val TextGrey = Color(0xFF9CA3AF)
